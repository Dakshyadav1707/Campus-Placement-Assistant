# Copy this file to ".env" (same folder) before running the backend.
# These are NOT secrets - just identifiers - so it's fine that they're
# pre-filled with your real project values here.

FOUNDRY_PROJECT_ENDPOINT=https://azure-campus-placement-resource.services.ai.azure.com/api/projects/AZURE-campus-placement
FOUNDRY_AGENT_NAME=CampusPlacementAgent
